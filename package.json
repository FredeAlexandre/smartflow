{
  "name": "smartflow",
  "private": true,
  "engines": {
    "node": ">=20.16.0"
  },
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "build": "nx run-many -t build -p website",
    "dev": "nx run-many -t dev -p website --watch"
  },
  "devDependencies": {
    "nx": "20.0.0",
    "typescript": "catalog:"
  }
}
